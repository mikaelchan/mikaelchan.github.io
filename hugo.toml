baseURL = 'http://blog.michaelchan.space/'
languageCode = 'zh-cn'
title = 'Zen & Programming'
theme = 'paper'

[params]
# color style
color = 'wheat' # linen, wheat, gray, light

# header social icons
github = 'mikaelchan' # github.com/YOUR_GITHUB_ID
rss = true            # show rss icon

# home page profile
avatar = 'hi@michaelchan.space' # gravatar email or image url
name = 'Michael Chan'
bio = 'less is more'


# misc
disableHLJS = false                            # disable highlight.js
disablePostNavigation = true                   # disable post navigation
monoDarkIcon = true                            # show monochrome dark mode icon
gravatarCdn = 'https://cdn.v2ex.com/gravatar/' # e.g. ''
math = true                                    # enable KaTeX math typesetting globally
localKatex = false                             # use local KaTeX js/css instead of CDN
favicon = "favicon.ico"                        # customize the default favicon
appleTouchIcon = "apple-touch-icon.png"        # customize the default Apple touch icon
